<% hub = metadata['requirements']['hub'] %>
<% spoke = metadata['requirements']['spoke'] %>
<% clouds = { "aws" => "Amazon Web Services", "gcp" => "Google Cloud Platform", "azure" => "Microsoft Azure" } %>
:_content-type: CONCEPT
:imagesdir: ../../images

[id="<%= metadata['pattern'] %>-openshift-hub-cluster-size"]
= <%= metadata['display_name'] %> hub cluster size minimum requirements

The <%= metadata['display_name'] %> pattern has been tested with a defined set of
specifically tested configurations that represent the most common combinations
that Red Hat OpenShift Container Platform customers are using or deploying for
the x86_64 architecture.

.Hub cluster minimum requirements
[cols="<,^,<,<"]
|===
| Cloud Provider | Node Type | Number of nodes | Instance Type
<% ['aws', 'gcp', 'azure'].each do |c| %>
| <%= clouds[c] %>
| Control Plane
| <%= hub['controlPlane']['platform'][c]['replicas'] %>
| <%= hub['controlPlane']['platform'][c]['type'] %>
| <%= clouds[c] %>
| Worker
| <%= hub['compute']['platform'][c]['replicas'] %>
| <%= hub['compute']['platform'][c]['type'] %>
<% end %>
|===

<% if !spoke.nil? and !spoke.empty? %>
{zwsp} +
{zwsp} +

[id="<%= metadata['pattern'] %>-openshift-spoke-cluster-size"]
= <%= metadata['display_name'] %> spoke cluster size minimum requirements
.Spoke cluster minimum requirements
[cols="<,^,<,<"]
|===
| Cloud Provider | Node Type | Number of nodes | Instance Type
<% ['aws', 'gcp', 'azure'].each do |c| %>
| <%= clouds[c] %>
| Control Plane
| <%= spoke['controlPlane']['platform'][c]['replicas'] %>
| <%= spoke['controlPlane']['platform'][c]['type'] %>
| <%= clouds[c] %>
| Worker
| <%= spoke['compute']['platform'][c]['replicas'] %>
| <%= spoke['compute']['platform'][c]['type'] %>
<% end %>
|===
<% end %>
